{
  "name": "reputation-scoring",
  "version": "0.1.0-beta.2",
  "description": "Library for calculating reputation scores based on GitHub activity.",
  "author": "Davide Ghiotto",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "format": "prettier --write .",
    "dev": "yarn build && source .env && node simulate.js",
    "build": "tsc",
    "package:preview": "npm pack --dry-run",
    "changeset": "npx changeset",
    "release": "changeset publish"
  },
  "type": "module",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "keywords": [],
  "dependencies": {
    "octokit": "^4.1.2"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.2",
    "@types/node": "^22.14.1",
    "dotenv-cli": "^8.0.0",
    "jest": "^29.7.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}

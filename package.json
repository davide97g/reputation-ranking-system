{
  "name": "reputation-scoring",
  "version": "0.0.5-beta.0",
  "description": "Library for calculating reputation scores based on GitHub activity.",
  "author": "Davide Ghiotto",
  "license": "MIT",
  "main": "dist/index.js",
  "scripts": {
    "format": "prettier --write .",
    "dev": "yarn build && source .env && node simulate.js",
    "build": "tsc",
    "package:preview": "npm pack --dry-run",
    "pre-release": "npm publish tag --beta",
    "release": "npm publish"
  },
  "type": "module",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "README.md"
  ],
  "keywords": [],
  "dependencies": {
    "octokit": "^4.1.2"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "dotenv-cli": "^8.0.0",
    "jest": "^29.7.0",
    "prettier": "^3.5.3",
    "typescript": "^5.8.3"
  }
}
